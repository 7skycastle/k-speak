/goal

# GOAL: 다국가용 한국어 학습 앱 `korean-first-talk` 신규 프로젝트 구현

이 문서를 처음부터 끝까지 읽은 뒤 작업을 시작한다. 일부 기능만 시각적으로 흉내 내는 시제품이 아니라, 신규 사용자가 실제로 온보딩을 마치고 Day 1 수업을 완료하며 복습 항목을 만들고, 중단 지점에서 재개하고, 로그인 후 다른 세션에서도 진도를 복구할 수 있는 앱을 구현한다.

작업 중 세부 사항이 명시되지 않았을 때는 아래 우선순위를 따른다.

1. 학습 흐름이 실제로 작동하는가
2. 사용자가 처음 3분 안에 듣거나 말할 수 있는가
3. 진도가 사라지지 않는가
4. 음성 재생과 녹음 비교가 안정적인가
5. 모바일에서 이해하기 쉽고 보기 좋은가
6. 국가팩과 Day 콘텐츠를 쉽게 추가할 수 있는가
7. 필요하지 않은 기능을 늘리지 않았는가

진행을 막는 정보가 아니라면 사소한 선택을 사용자에게 계속 묻지 말고 합리적인 기본값으로 진행한다. 다만 새 Supabase 생성, 상용 배포, 유료 서비스 연결, 상용 음원 라이선스 승인처럼 사용자 권한이나 명시적 판단이 필요한 일은 임의로 처리하지 않는다.

---

## 1. 최종 제품 결정

### 1.1 이번에 만들 제품

이번 결과물은 `14일 First Talk 프로그램`을 포함한 출시 전 검증용 완성 앱이다.

Day 1~14만으로 한국어 전체 과정을 완성했다고 표현하지 않는다. 대신 다음 약속을 완결성 있게 제공한다.

> 14일 동안 외국인이 한국어를 처음 듣고, 따라 말하고, 기본적인 실제 상황에서 짧게 대답할 수 있도록 돕는다.

Day 1~14가 끝났을 때 사용자는 다음을 할 수 있어야 한다.

- 기본 인사와 자기소개
- 감사·사과·간단한 반응
- 카페와 식당에서 주문
- 편의점과 상점에서 결제·가격 질문
- 길과 교통수단 질문
- 시간과 약속 표현
- 취향과 관심사 말하기
- 도움 요청과 기본적인 문제 설명
- 존댓말로 짧은 부탁
- 여러 상황을 연결한 최종 역할극

### 1.2 전체 콘텐츠 확장 계획

이번 개발에서는 Day 1~14까지 실제 구현하고, 아래 과정은 데이터 구조와 문서만 준비한다.

- Day 15~30: 한국 여행·생활 생존 회화
- Day 31~60: 일상 대화와 문장 확장
- Day 61~90: 친구·학교·직장·감정 표현
- 목표팩: 여행, K-콘텐츠, 유학, 취업, TOPIK, EPS-TOPIK
- 별도 기초팩: 한글, 받침, 연음, 숫자, 존댓말

Day 15 이후를 이번 범위에 무리하게 넣지 않는다. Day 1~14의 완료율, 녹음 사용률, 복습률, 재방문율을 확인한 뒤 확장할 수 있도록 구조만 열어 둔다.

### 1.3 첫 국가 버전

공통 한국어 콘텐츠와 학습 엔진은 하나만 만든다.

- 기본 공통 언어팩: 영어 `en`
- 첫 정식 국가팩: 일본 `ja-JP`
- 다음 확장 순서: 대만 `zh-TW` → 베트남 `vi-VN` → 인도네시아 `id-ID` → 태국 `th-TH` → 중국 본토 `zh-CN`

중국 본토는 앱 마켓, 로그인, 결제, 개인정보, 서버 환경이 다른 국가와 다르므로 마지막에 별도 배포 계획을 세운다.

완성되지 않은 언어팩은 사용자 선택 화면에 노출하지 않는다. 빈 번역이나 자동 번역 문구를 제품 화면에 넣지 않는다.

---

## 2. 프로젝트 경계와 저장소 기준

이번 작업은 완전히 새로운 프로젝트 기준이다.

- 제품명: `korean-first-talk`
- GitHub 저장소: `https://github.com/7skycastle/k-speak`
- 기존 배포 주소 `https://korean-first-talk.sunset-root-5760.chatgpt.site`는 사용하지 않는다.
- 기존 Supabase 프로젝트 `K_study`는 사용하지 않는다.
- 기존 Supabase 프로젝트 ID `bcpoowcqvqynpciiqoav`는 코드, 문서, 환경 변수 어디에도 사용하지 않는다.
- 기존 Sites 프로젝트, 기존 테이블, 기존 정책, 기존 음원에 의존하지 않는다.
- 공개 배포, 유료 결제, 실제 푸시 알림 발송은 별도 승인 없이 진행하지 않는다.

### 2.1 저장소 점검

작업을 시작하면 먼저 다음을 확인하고 `docs/REPO_AUDIT.md`에 간단히 기록한다.

- 현재 디렉터리가 올바른 저장소인지
- 기본 브랜치와 현재 브랜치
- 수정되었지만 커밋되지 않은 파일
- `AGENTS.md`, `README`, 기존 설계 문서
- `package.json`과 잠금 파일
- 사용 중인 프레임워크와 실행 명령
- 기존 라우트, 컴포넌트, 상태 저장 방식
- Supabase 또는 다른 외부 서비스 연결 흔적
- 테스트와 CI 설정
- 기존 Day 콘텐츠와 음원 존재 여부

저장소에 정상 작동하는 코드가 있으면 보존하고 이번 구조로 점진적으로 옮긴다. 사용자가 만든 파일이나 관련 없는 변경을 삭제하거나 되돌리지 않는다.

저장소가 비어 있으면 아래 권장 기술 구성으로 새로 시작한다.

### 2.2 Git 작업 방식

- 기능 브랜치 예시: `feat/first-talk-foundation`
- 직접 `main`에 덮어쓰지 않는다.
- 기능 단위로 의미 있는 커밋을 남긴다.
- 권한이 있으면 기능 브랜치를 push하고 Draft PR을 만든다.
- 병합과 상용 배포는 하지 않는다.
- 권한이 없으면 로컬 커밋까지 만들고 정확한 상태를 보고한다.

---

## 3. 권장 기술 구성

저장소에 이미 안정적인 기술 구성이 있으면 불필요하게 교체하지 않는다. 비어 있거나 초기 상태라면 다음을 권장한다.

- Next.js App Router
- TypeScript strict mode
- Tailwind CSS와 소수의 접근성 높은 UI primitive
- Zod 기반 콘텐츠·환경 변수 검증
- IndexedDB 기반 로컬 우선 저장. 필요하면 Dexie 사용
- Supabase JavaScript client
- Vitest 또는 현재 저장소의 단위 테스트 도구
- Playwright E2E 테스트
- ESLint, TypeScript 검사, production build 검증

의존성은 필요한 만큼만 추가한다. 단순한 재생 버튼이나 상태 표시를 위해 크고 무거운 라이브러리를 넣지 않는다.

### 3.1 앱 경로 권장안

```text
/
/onboarding
/home
/learn
/learn/day-01
/review
/settings
/auth
```

첫 방문자는 `/onboarding`, 온보딩 완료자는 `/home`, 진행 중인 수업이 있으면 홈의 이어하기 버튼을 통해 정확한 단계로 이동한다.

### 3.2 권장 디렉터리

기존 구조와 충돌하지 않는 범위에서 다음과 유사하게 구성한다.

```text
src/
  app/
  components/
    app-shell/
    onboarding/
    lesson/
    audio/
    review/
  content/
    core/
    locales/
      en/
      ja-JP/
    country-packs/
  domain/
    lessons/
    progress/
    review/
    sync/
  lib/
    audio/
    storage/
    supabase/
    analytics/
  stores/
public/
  audio/
  characters/
tools/
  tts/
supabase/
  migrations/
docs/
```

UI 컴포넌트, 학습 규칙, 저장 방식, 콘텐츠 데이터를 분리한다. Day를 추가하기 위해 화면 코드를 복사하지 않게 한다.

---

## 4. 음성 방식에 대한 최종 결정

### 4.1 채택 방식

정규 수업의 고정 문장은 미리 생성하고 검수한 정적 AI 음원을 사용한다. 모든 고정 문장을 재생할 때마다 실시간 TTS로 생성하지 않는다.

최종 구성은 다음과 같다.

| 용도 | 방식 |
|---|---|
| Day 1~14 핵심 문장 | 정적 AI 음원 |
| 자연 속도·느린 속도 | 각각 정적 AI 음원 |
| 어절 반복 | 정적 분리 음원 또는 검수된 구간 정보 |
| 역할극 고정 대사 | 정적 AI 음원 |
| 캐릭터 안내·칭찬 | 공통 정적 음원 라이브러리 |
| 복습 문장 | 기존 정적 음원 재사용 |
| 사용자가 입력한 임의 문장 | 브라우저 `speechSynthesis` 임시 재생 |
| 음원 누락 | 브라우저 TTS fallback과 명확한 개발 경고 |
| 향후 자유대화·개인화 문장 | 실시간 TTS 또는 최초 1회 생성 후 캐시 |

### 4.2 이 방식을 선택하는 이유

- 같은 문장이 언제나 같은 발음과 억양으로 재생된다.
- 반복 버튼을 눌렀을 때 즉시 재생된다.
- 오프라인 또는 불안정한 네트워크에서도 사용할 수 있다.
- 무료 모델을 개발 도구에서 한 번만 실행하므로 운영 서버 비용이 없다.
- 잘못 생성된 문장만 찾아 교체할 수 있다.
- 발음 비교의 기준 음성이 버전 변경으로 달라지지 않는다.
- 국가팩이 추가되어도 한국어 음원을 다시 생성할 필요가 없다.

### 4.3 유료 TTS 금지

다음 유료·상용 API는 사용하지 않는다.

- ElevenLabs
- OpenAI TTS
- Google Cloud TTS
- Azure TTS
- Naver Clova Voice
- 그 밖의 문자 수 또는 호출 수에 따라 요금이 발생하는 TTS API

실시간 TTS 서버도 이번 버전에서는 만들지 않는다. 향후 자유대화 기능을 만들 때 별도 검토한다.

### 4.4 무료 오픈소스 TTS 평가 절차

특정 모델을 이름만 보고 바로 정식 음원에 사용하지 않는다. 다음 세 가지를 별도로 확인한다.

1. 실행 코드 라이선스
2. 모델 가중치 라이선스
3. 학습 데이터의 출처와 상업적 사용 조건

세 항목 중 하나라도 불명확하면 `commercialUse: approved`로 표시하지 않는다.

KSS Dataset 또는 CC BY-NC-SA처럼 비상업 조건이 포함된 데이터로 학습한 모델은 상용 공개 음원에 사용하지 않는다.

개발용 비교 후보는 다음과 같이 둘 수 있지만, 후보라는 이유만으로 승인된 것으로 취급하지 않는다.

- Qwen3-TTS 0.6B 계열: 한국어와 음성 스타일을 지원하므로 품질 비교 후보
- MeloTTS Korean: 로컬 CPU 실행이 비교적 쉬우므로 개발 편의 후보
- 그 밖의 신규 모델: 라이선스와 한국어 품질을 같은 기준으로 확인한 뒤 추가

각 후보로 짧은 테스트 음원만 먼저 만든다. 최소 테스트 문장은 다음 특징을 포함한다.

- 평음·경음·격음
- 받침
- 연음
- 숫자
- 외래어
- 존댓말 어미
- 질문 억양
- 짧은 감탄과 자연스러운 대화체

테스트 음원을 비교한 뒤 다음 기준을 모두 통과한 모델만 정식 생성에 사용한다.

- 한국어가 자연스럽고 명료함
- 문장 끝 억양이 과장되거나 기계적이지 않음
- 느린 속도에서도 음절이 무너지지 않음
- 4개 캐릭터의 음색이 안정적으로 재현됨
- 코드·가중치·학습 데이터 조건이 기록됨
- 생성 음원을 상업적 앱의 정적 파일로 배포할 수 있음

라이선스가 해결되지 않은 상태에서도 앱 개발은 계속할 수 있다. 이 경우 정적 테스트 파일 또는 브라우저 fallback으로 기능을 검증하되, 정식 음원처럼 표시하거나 상용 배포하지 않는다.

### 4.5 음원 생성 도구

`tools/tts` 아래에 다음을 구현한다.

```text
tools/tts/
  README.md
  requirements.txt 또는 pyproject.toml
  voices.json
  sentences.json
  licenses.json
  generate_audio.py
  validate_audio.py
  generate.ps1
```

필요 조건:

- Windows 로컬 환경에서 실행 가능한 PowerShell 명령 제공
- 모델 파일과 캐시는 Git에 올리지 않음
- 동일 문장은 콘텐츠 해시로 중복 생성 방지
- 일부 문장만 선택 생성 가능
- 전체 Day를 일괄 생성 가능
- 실패한 문장 목록 출력
- 음원 누락, 0초 파일, 비정상 길이 검사
- 생성 후 manifest 자동 갱신
- 원본 WAV 보관 여부와 앱용 MP3 변환을 구분
- 음량을 일정하게 정규화
- 모델명, 버전, voice ID, 생성일, 텍스트 해시 기록

자연 속도는 기본 속도로 생성한다. 느린 음원은 약 `0.82~0.90` 범위에서 별도로 생성하고 품질을 확인한다. 지나치게 늘려 발음이 깨지는 음원은 사용하지 않는다.

앱용 음원은 말하기 학습에 충분한 수준의 mono MP3로 만들고 초기 로딩을 줄인다. 현재 문장과 다음 문장 정도만 미리 읽고 전체 과정의 음원을 한꺼번에 내려받지 않는다.

### 4.6 음원 파일 구조와 메타데이터

```text
public/audio/
  day-01/
    haneul/
      phrase-001-natural.mp3
      phrase-001-slow.mp3
    jun/
    mina/
    taeho/
```

예시 메타데이터:

```json
{
  "audioId": "day-01.phrase-001.haneul.natural.v1",
  "lessonId": "day-01",
  "phraseId": "phrase-001",
  "characterId": "haneul",
  "speed": "natural",
  "sourceType": "ai_generated_static",
  "provider": "local_open_source",
  "modelId": "candidate_model_id",
  "voiceId": "voice_profile_id",
  "modelVersion": "model_version",
  "audioVersion": "v1",
  "textHash": "sha256",
  "codeLicense": "pending_or_verified",
  "weightLicense": "pending_or_verified",
  "trainingDataLicense": "pending_or_verified",
  "licenseStatus": "pending_review",
  "commercialUse": false,
  "reviewStatus": "not_reviewed"
}
```

`commercialUse`는 근거 문서가 확인되기 전까지 기본값을 `false`로 둔다. 음원을 다시 만들었을 때는 파일을 조용히 덮어쓰지 말고 `audioVersion`을 올린다.

### 4.7 음원 수를 효율적으로 관리하는 방법

- Day별 핵심 표현은 4~6개로 제한한다.
- 공통 안내와 칭찬은 Day마다 만들지 말고 공통 음원 라이브러리로 재사용한다.
- 같은 한국어 문장이 여러 Day에 나오면 동일 `phraseId`와 음원을 재사용한다.
- 역할극 음성은 해당 장면에 실제 등장하는 캐릭터만 생성한다.
- Day 1~14 단계에서는 저장소의 `public/audio`에 넣되, 전체 음원이 지나치게 커지면 manifest URL을 유지한 채 새 Supabase Storage 또는 별도 정적 스토리지로 이전할 수 있게 한다.

---

## 5. 캐릭터와 AI 음성

기본 캐릭터는 4명이다. 실존 인물이나 연예인의 목소리를 복제하지 않는다.

### 하늘

- 역할: 친절한 첫 한국어 튜터
- 음성 느낌: 밝고 부드러운 여성 음색
- 속도: 보통보다 약간 느림
- 사용: 온보딩, Day 1 안내, 칭찬, 복습 안내

### 준

- 역할: 편한 한국 친구
- 음성 느낌: 자연스럽고 일상적인 남성 음색
- 속도: 실제 회화에 가까움
- 사용: 일상 역할극과 복귀 안내

### 미나

- 역할: 또렷한 발음 코치
- 음성 느낌: 명확하고 정돈된 여성 음색
- 속도: 문장 구조가 잘 들리는 속도
- 사용: 발음 연습과 느린 음성

### 태호

- 역할: 차분한 설명자
- 음성 느낌: 낮고 안정적인 남성 음색
- 속도: 천천히
- 사용: 힌트와 어려운 문장 재확인

사용자가 선택한 캐릭터는 홈, 안내 문구, 수업 진행 음성에 실제로 반영되어야 한다. 이름과 그림만 바뀌고 목소리가 그대로인 가짜 선택 기능을 만들지 않는다.

Day 1~14에서 네 캐릭터의 모든 조합을 무조건 생성하기 전에 manifest를 기준으로 실제 필요한 음원 수를 계산한다. 핵심 문장에는 네 캐릭터의 자연·느린 버전을 제공할 수 있지만, 장면 대화와 공통 안내는 재사용하여 파일 증가를 줄인다.

---

## 6. 콘텐츠 데이터 구조

Day 화면마다 별도 코드를 만들지 않는다. 하나의 공통 수업 엔진이 콘텐츠 데이터를 읽어 모든 Day를 렌더링해야 한다.

### 6.1 수업 단계 유형

최소 다음 유형을 지원한다.

```text
scenario
character_intro
listen
meaning
word_breakdown
repeat
record_compare
comprehension
roleplay
summary
review_schedule
next_preview
```

### 6.2 필수 콘텐츠 필드

각 수업과 문장은 안정적인 ID를 가져야 한다.

```text
lessonId
contentRevision
dayNumber
title
situation
estimatedMinutes
goalTags
stepId
stepType
phraseId
koreanText
translationKey
romanization
romanizationPolicy
wordBreakdown
grammarNoteKey
pronunciationNoteKey
audioRefs
hintKey
difficulty
reviewEligible
countryOverrides
```

진도는 배열 순번만 저장하지 말고 `lessonId`, `stepId`, `contentRevision`으로 저장한다. 콘텐츠 순서가 바뀌어도 가능한 한 기존 진도를 복구할 수 있어야 한다.

### 6.3 콘텐츠 파일 분리

```text
src/content/core/day-01.json
src/content/locales/en/day-01.json
src/content/locales/ja-JP/day-01.json
src/content/country-packs/ja-JP.json
```

- `core`: 한국어 문장, 단계, 정답, phrase ID, 음원 참조
- `locales`: 번역, 설명, 버튼 문구
- `country-packs`: 국가별 목적, 문화 메모, 발음 주의점, 상황 변경

한국어 문장과 음원은 국가별로 복제하지 않는다.

모든 콘텐츠는 Zod 또는 동등한 방식으로 build 전에 검증한다. 번역 누락, 중복 ID, 잘못된 음원 경로가 있으면 build 또는 별도 검증 명령에서 명확하게 실패해야 한다.

### 6.4 Day 1~14 권장 주제

1. 인사하고 이름 말하기
2. 감사·사과·기본 반응
3. 카페에서 주문하기
4. 식당에서 메뉴와 요청 말하기
5. 편의점에서 결제하기
6. 쇼핑하며 가격·수량 묻기
7. 길 묻고 방향 이해하기
8. 지하철·버스 이용하기
9. 시간과 약속 정하기
10. 좋아하는 것과 K-콘텐츠 말하기
11. 한국 친구와 짧은 대화하기
12. 문제를 설명하고 도움 요청하기
13. 존댓말로 부탁하기
14. 여러 상황을 연결한 최종 역할극

기존 저장소에 이미 검토된 Day 콘텐츠가 있다면 그대로 폐기하지 말고 위 구조에 맞게 매핑한다. 어색하거나 학습 가치가 낮은 문장만 수정하고 변경 이유를 기록한다.

각 Day 권장 분량:

- 핵심 표현 4~6개
- 짧은 대화 1개
- 이해 확인 1~2개
- 녹음 비교 1회 이상
- 역할극 1개
- 요약과 복습 예약
- 예상 학습 시간 3~7분

문장을 많이 넣는 것보다 실제로 듣고 따라 말하고 다시 쓸 수 있게 만드는 것을 우선한다.

---

## 7. 국가팩 설계

국가별 앱을 코드 복사본으로 만들지 않는다. 공통 코드 약 70%, 언어별 설명 약 20%, 국가별 상황·목표 약 10%를 기준으로 한다.

국가팩은 최소 다음을 설정할 수 있어야 한다.

```text
locale
countryCode
displayName
learningPurposeOptions
defaultDailyGoal
romanizationPolicy
grammarComparisonNotes
pronunciationNotes
culturalNotes
scenarioOverrides
examMappings
contentVisibility
```

### 7.1 일본 국가팩 필수 요소

- 일본어와 한국어의 비슷한 어순을 이용한 설명
- 조사와 존댓말 비교
- 받침과 연음 설명
- 초반에만 일본어식 발음 힌트를 선택적으로 제공
- 자연 속도, 느린 속도, 어절 반복
- 전체 대화를 화면을 보지 않고 듣는 모드
- 문장 복사와 저장
- 여행, 카페, 쇼핑, 콘서트, 친구 대화 상황
- 긴 문법 강의보다 짧은 설명과 예시

발음 힌트는 영구 의존 기능이 되지 않도록 사용자가 단계적으로 숨길 수 있게 한다.

### 7.2 영어 공통팩 필수 요소

- 짧고 명확한 영어 설명
- 로마자는 한글 입문 초기에만 선택적으로 제공
- 설명보다 상황과 말하기를 우선
- 다양한 문화권에서 오해하지 않는 중립적인 예시

### 7.3 향후 국가팩 원칙

- 대만: 번체중문, 한자어 연결, 여행과 K-콘텐츠
- 베트남: TOPIK·EPS-TOPIK, 유학·직장·생활 표현
- 인도네시아: 가벼운 데이터 사용, 오프라인, 유학·취업·K-콘텐츠
- 태국: 평음·경음·격음과 종성, 여행·드라마·팬 활동
- 중국 본토: 간체중문, 한자어, TOPIK, 교재 연결, 별도 배포 인프라

국가 문화를 단순한 고정관념으로 표현하지 않는다. 번역과 문화 설명에는 `reviewStatus`를 두고 현지 검수 여부를 명확히 기록한다.

---

## 8. 필수 사용자 흐름

### 8.1 온보딩

사용자는 로그인 없이 60초 안에 설정하고 Day 1로 이동할 수 있어야 한다.

1. 모국어와 국가
2. 한국어 수준
3. 학습 목적
4. 하루 목표: 3분·5분·10분·15분
5. 캐릭터와 음성
6. 선호 학습 시간 또는 알림 시간
7. 결과 요약
8. Day 1 시작

원칙:

- 한 화면에 핵심 선택 하나
- 진행 단계 표시
- 필수 항목과 건너뛸 수 있는 항목 구분
- 뒤로 가도 선택 유지
- 완료 후 재방문하면 홈으로 이동
- 설정에서 나중에 변경 가능
- 실제 알림 발송은 이번 범위에서 제외하고 선호값만 저장

### 8.2 홈과 하단 메뉴

하단 메뉴는 다음 네 개만 둔다.

- 홈
- 학습
- 복습
- 내 정보 또는 설정

홈 필수 정보:

- 오늘 이어서 할 수업
- 오늘 복습할 문장 수
- 선택한 캐릭터
- 현재 국가팩
- 하루 학습 목표
- 최근 학습 상태
- Day 시작 또는 이어하기
- 비회원 진도 저장 상태

모바일 화면에서 한 번에 너무 많은 카드가 보이지 않게 한다. 가장 중요한 `이어하기`를 한 개의 주 행동으로 두고, 나머지는 보조 정보로 정리한다.

### 8.3 Day 1 기준 수업 흐름

1. 실제 상황 제시
2. 캐릭터의 짧은 인사
3. 핵심 한국어 문장
4. 모국어 의미
5. 자연 속도 듣기
6. 느린 속도 듣기
7. 문장 또는 어절 반복
8. 사용자 녹음
9. AI 원본과 내 목소리 비교
10. 다시 녹음 또는 건너뛰기
11. 짧은 이해 문제
12. 실제 대화 역할극
13. 오늘 표현 정리
14. 복습 예약
15. 다음 Day 예고

Day 1을 완성하기 전에 Day 2 화면을 복사해 늘리지 않는다. Day 1이 공통 엔진의 기준 사례가 되어야 한다.

---

## 9. 녹음과 발음 비교

### 9.1 핵심 화면

한 화면에서 다음 흐름이 분명하게 보여야 한다.

```text
AI 원본 듣기 → 녹음하기 → 내 목소리 듣기 → 번갈아 듣기 → 다시 녹음
```

필수 기능:

- 자연 속도와 느린 속도 선택
- 원본과 내 녹음의 독립적인 재생 상태
- 두 음성을 순서대로 자동 재생하는 비교 버튼
- 재생 진행 상태
- 녹음 중 상태와 시간
- 재녹음
- 녹음 건너뛰기
- 마이크 권한 거절 시 해결 안내
- 지원하지 않는 브라우저에서 대체 흐름

파형은 이해에 도움이 되는 간단한 수준으로 구현한다. 파형 때문에 녹음 기능 전체가 불안정해지면 우선 재생 진행 막대와 명확한 상태 표시를 사용한다.

### 9.2 지켜야 할 원칙

- 신뢰할 수 없는 0~100 발음 점수를 표시하지 않는다.
- 음성 인식 결과가 맞지 않는다고 학습 완료를 막지 않는다.
- 사용자의 발음이 틀렸다고 단정하지 않는다.
- 녹음 파일은 기본적으로 브라우저 안에서만 사용한다.
- 명시적 동의 없이 원본 음성을 서버에 업로드하지 않는다.
- 기본 저장 데이터에는 녹음 시도 횟수와 완료 여부만 남긴다.
- iOS Safari, Android Chrome 등에서 지원 MIME type을 감지한다.
- 녹음 object URL과 메모리를 적절히 해제한다.

---

## 10. 초기 이탈을 줄이는 흐름

다음 경험을 하나로 연결한다.

```text
앱 실행
→ 짧은 선택
→ 3분 안에 첫 한국어 문장 듣기 또는 말하기
→ 즉시 이해 가능한 성공 경험
→ 짧은 역할극
→ 원본과 내 발음 비교
→ 다음 학습 약속
```

적용 기능:

- 가입 전 학습
- 첫 문제의 낮은 난이도
- Day 1의 긴 문법 설명 금지
- 오답이어도 흐름 유지
- 힌트와 느린 음성
- 정확한 단계에서 재시작
- 하루 목표 변경
- 수업 종료 직전 다음 Day의 흥미 요소 제시
- Day 1 완료 후 진도 보관을 위한 로그인 제안
- 연속 기록을 놓친 사용자에게 부담을 주지 않는 복귀 문구
- 장기간 미접속 사용자를 위한 1~2분 재시작 복습
- 반복 팝업, 의미 없는 코인, 지나친 축하 화면 금지

로그인은 가치를 경험하기 전에 강요하지 않는다.

### 10.1 측정 이벤트

외부 분석 도구를 바로 연결하지 않아도 이벤트 구조는 준비한다.

```text
onboarding_started
onboarding_completed
lesson_started
first_audio_played
recording_started
recording_completed
recording_skipped
microphone_denied
lesson_step_completed
lesson_resumed
lesson_completed
review_item_created
review_session_completed
login_prompt_seen
auth_completed
sync_completed
```

이벤트에 원본 녹음, 이메일, 번역되지 않은 개인정보를 넣지 않는다.

---

## 11. 맞춤 복습 엔진

처음에는 설명 가능한 규칙 기반으로 구현한다.

복습 우선순위 입력값:

- 오답 여부
- 답변 시간
- 힌트 사용
- 자연 음성 재생 횟수
- 느린 음성 사용
- 녹음 재시도 횟수
- 사용자가 표시한 어려운 문장
- 마지막 학습 후 경과 시간
- 이전 복습 결과

가중치는 코드에 흩어 놓지 말고 별도 설정 파일로 분리한다.

예시 방향:

```text
어려움 직접 표시: 매우 높음
이전 복습 실패: 높음
첫 오답: 높음
녹음 여러 번 재시도: 중간
힌트 사용: 중간
느린 음성 사용: 낮음
오래 지난 문장: 경과일에 따라 증가
```

필수 기능:

- 오늘 복습 수 표시
- 3분 내외 세션
- 듣기·의미·말하기 혼합
- 복습 완료 후 다음 시점 계산
- 복습할 내용이 없으면 신규 수업 안내
- 복습 이유 한 줄 표시
- 언어팩을 바꿔도 같은 한국어 phrase의 학습 기록 유지
- `(사용자, phraseId)` 기준 중복 방지

예시 문구:

- 어제 어려워했던 표현이에요.
- 여러 번 다시 들었던 문장이에요.
- 며칠 전에 배운 표현을 확인해요.

초기 복습 간격은 성공 정도에 따라 `1일 → 3일 → 7일 → 14일 → 30일`처럼 단순하고 검증 가능하게 구성한다.

---

## 12. 로컬 저장, 인증, Supabase 동기화

### 12.1 새 Supabase만 사용

새 Supabase URL과 anon key는 환경 변수로 받는다.

```text
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
```

- `.env.example`만 커밋한다.
- 실제 키를 저장소에 넣지 않는다.
- service role key를 브라우저 코드에 절대 넣지 않는다.
- 새 프로젝트 정보가 없으면 로컬 모드로 완성하고 SQL·연결 문서까지 제공한다.
- 새 Supabase 프로젝트 자체를 만들 권한이 없으면 생성하지 말고 필요한 값을 요청한다.

### 12.2 로컬 우선 저장

비회원 데이터는 IndexedDB에 저장한다.

- 익명 사용자 ID
- 온보딩 설정
- 국가팩과 언어
- 캐릭터
- 하루 목표
- 수업별 현재 step ID
- 완료 step ID 목록
- 완료한 Day
- 복습 항목
- 동기화 대기 이벤트

페이지를 새로고침하거나 브라우저를 종료해도 중단 단계에서 재개되어야 한다.

### 12.3 권장 Supabase 테이블

최소한 다음 구조를 마이그레이션 SQL로 제공한다.

```text
profiles
user_preferences
lesson_progress
review_items
learning_events
```

필요한 경우 `sync_merge_logs`를 추가할 수 있지만 불필요하게 테이블을 늘리지 않는다.

모든 사용자 데이터 테이블은 `user_id`를 기준으로 RLS를 적용한다. 사용자는 자신의 데이터만 읽고 쓸 수 있어야 한다.

### 12.4 비회원 진도 병합

검증해야 할 실제 흐름:

1. 비회원 온보딩
2. Day 1 일부 진행
3. 앱 종료
4. 다시 실행
5. 정확한 단계에서 재개
6. Day 1 완료
7. 이메일 로그인
8. 로컬 진도와 계정 진도 병합
9. 로그아웃
10. 다시 로그인
11. 완료 진도와 복습 복구
12. 다른 브라우저 또는 새 세션 로그인
13. 클라우드 진도 복구
14. 국가팩·캐릭터·목표 복구

병합 원칙:

- 완료 기록은 합집합으로 보존
- 진행 단계는 동일 콘텐츠 revision 안에서 더 많이 진행한 쪽 보존
- 최초 시작일은 더 이른 값, 최근 활동일은 더 늦은 값 보존
- 복습 항목은 phrase ID로 합치고 더 이른 due date 또는 더 어려운 상태를 안전하게 보존
- 사용자 설정은 가장 최근에 사용자가 명시적으로 바꾼 값 사용
- 같은 동기화 요청이 반복되어도 중복 데이터가 생기지 않도록 idempotent upsert 사용
- 네트워크 실패 시 로컬 queue에 남기고 재연결 후 재시도
- 병합 결과를 짧게 안내

이메일 인증은 Supabase에서 안정적으로 제공되는 간단한 방식으로 구현한다. 초기에는 비밀번호 재설정 흐름이 필요한 복잡한 가입보다 이메일 OTP 또는 magic link를 우선 검토한다. 실제 새 Supabase 설정과 사용자의 요구가 있으면 비밀번호 로그인을 추가한다.

---

## 13. UI·UX 품질 기준

다른 학습 앱의 장점을 참고하되 화면, 문구, 그래픽, 고유 자산을 복제하지 않는다.

흡수할 원칙:

- TEUIDA의 빠른 말하기 시작과 상황 몰입
- LingoDeer의 명확한 과정과 설명
- Nemo의 원본·내 녹음 비교
- Cake의 흥미 있는 상황 제시
- 현지 단어 앱의 분명한 학습량과 복습

디자인 기준:

- 모바일 우선, 360~430px에서 먼저 완성
- 데스크톱에서는 지나치게 늘리지 않고 중앙 앱 프레임 사용
- 하단 메뉴가 콘텐츠를 가리지 않음
- 터치 영역 최소 약 44px
- 한 화면의 주 행동은 하나
- 제목, 설명, 학습 문장, 버튼의 시각적 순서가 명확함
- 카드와 테두리를 과도하게 사용하지 않음
- 여백과 글자 크기로 영역 구분
- 밝고 친근하지만 유아용처럼 보이지 않음
- 장식보다 한국어 문장과 재생·녹음 기능을 우선
- 로딩, 빈 상태, 오류, 권한 거절 상태까지 디자인
- 키보드 탐색과 기본적인 스크린리더 label 지원
- 색상만으로 정답·오답을 구분하지 않음
- 의미 없는 버튼과 연결되지 않은 메뉴 금지

캐릭터 이미지는 독창적인 자산을 사용한다. 아직 최종 그림이 없으면 교체 가능한 정돈된 임시 아바타를 사용하되, 이모지 하나를 최종 캐릭터처럼 놓고 완료 처리하지 않는다.

---

## 14. 권장 개발 순서

아래 순서를 지킨다. 뒤 단계의 기능을 먼저 많이 만들기보다 각 단계가 실제로 작동하는지 검증한 뒤 확장한다.

### 단계 0. 저장소와 실행 환경 점검

작업:

- 저장소, 브랜치, 변경 파일 확인
- 기존 앱 실행
- 기존 라우트·콘텐츠·음원·Supabase 연결 확인
- 테스트와 build 실행
- `docs/REPO_AUDIT.md` 작성

완료 기준:

- 무엇을 보존하고 무엇을 새로 만들지 명확함
- 기존 오류가 있다면 재현 방법이 기록됨
- 실행 명령이 확인됨

### 단계 1. 제품 구조와 데이터 계약 확정

작업:

- 콘텐츠 schema
- locale·country-pack schema
- audio manifest schema
- progress·review schema
- 캐릭터 metadata
- Day 1 실제 콘텐츠 초안
- `docs/PRODUCT_SPEC.md`, `docs/ARCHITECTURE.md` 작성

완료 기준:

- 코드 복사 없이 Day 추가 가능
- 한국어 core와 번역이 분리됨
- 모든 ID 규칙이 확정됨
- 잘못된 콘텐츠를 자동 검사할 수 있음

### 단계 2. 앱 기본 틀과 디자인 시스템

작업:

- 모바일 앱 shell
- 하단 메뉴
- 공통 header, button, progress, audio control
- 색상·글자·간격 token
- 홈·학습·복습·설정의 실제 route
- 빈 메뉴가 없도록 최소 기능 연결

완료 기준:

- 모바일과 데스크톱에서 깨지지 않음
- 네 메뉴가 실제 화면으로 이동함
- 선택 상태가 분명함
- 임시 랜딩 페이지가 첫 화면을 대신하지 않음

### 단계 3. 온보딩과 비회원 프로필

작업:

- 60초 온보딩
- 국가·언어·수준·목적·목표·캐릭터·시간 선택
- 설정 요약
- IndexedDB 저장
- 재방문 분기
- 설정 변경

완료 기준:

- 로그인 없이 완료 가능
- 새로고침해도 선택 유지
- 완료 후 Day 1로 이동
- 재방문 시 홈으로 이동
- 선택 캐릭터와 국가팩이 홈에 반영

### 단계 4. Day 1 공통 수업 엔진 완성

작업:

- 모든 lesson step renderer
- 자연·느린 재생 UI
- 어절 반복
- 의미·단어·짧은 설명
- 이해 문제
- 역할극
- 결과·복습 예약·다음 예고
- 각 step 완료 상태 저장

완료 기준:

- Day 1을 처음부터 끝까지 완료 가능
- 중간 새로고침 후 정확한 step에서 재개
- 오답과 힌트가 흐름을 막지 않음
- 녹음이 불가능해도 Day 1 완료 가능
- Day 2를 만들기 위해 새 화면 컴포넌트가 필요하지 않음

### 단계 5. 무료 정적 TTS 파이프라인과 시험 음원

작업:

- 후보 모델 라이선스 표 작성
- Windows 로컬 생성 도구
- 대표 문장 audition pack 생성
- 자연·느린 속도 비교
- 네 캐릭터 음성 일관성 확인
- audio manifest와 검증 스크립트
- `docs/AUDIO_PIPELINE.md`, `docs/AUDIO_LICENSES.md`

완료 기준:

- 유료 API를 호출하지 않음
- 코드·가중치·학습 데이터 조건이 분리되어 기록됨
- 정적 음원을 앱에서 재생할 수 있음
- 누락 음원은 자동 검출됨
- 승인되지 않은 음원이 상용 가능으로 표시되지 않음

라이선스나 음색 선택을 위해 사용자 확인이 필요하면 audition 파일과 비교표를 제시한다. 답변을 기다리는 동안 다른 단계는 계속 진행한다.

### 단계 6. 녹음과 원본 비교 완성

작업:

- 마이크 권한 요청
- MediaRecorder 지원 감지
- 녹음·재생·재녹음
- 원본과 내 녹음 번갈아 듣기
- 간단한 진행 표시 또는 파형
- 권한 거절·미지원·오류 상태
- 녹음 기본 비업로드 정책

완료 기준:

- 지원 브라우저에서 녹음과 비교가 실제 작동
- 권한 거절 후 막힌 화면이 되지 않음
- 임의 발음 점수가 없음
- 녹음 파일이 Supabase로 전송되지 않음

### 단계 7. 복습과 초기 이탈 방지 흐름

작업:

- 규칙 기반 복습 우선순위
- review item 생성·중복 방지
- 3분 복습 세션
- 복습 이유 표시
- Day 1 완료 직후 로그인 제안
- 정확한 이어하기
- 복귀 사용자 안내
- 이벤트 기록

완료 기준:

- Day 1 행동에 따라 복습 항목이 달라짐
- 오늘 복습 수가 홈과 일치
- 복습 완료 후 다음 날짜가 계산됨
- 복습 데이터가 새로고침 후 유지됨

### 단계 8. 새 Supabase 인증·동기화

작업:

- 새 프로젝트용 migration SQL
- RLS 정책
- Supabase client와 환경 변수
- 이메일 인증
- 비회원 데이터 업로드·병합
- offline queue와 재시도
- 동기화 상태 UI
- 설정 복구

완료 기준:

- 로컬 모드는 Supabase 없이도 작동
- 새 Supabase 정보가 제공되면 실제 연결 검증
- 로그아웃·재로그인 후 진도 복구
- 다른 세션에서 복구
- RLS로 다른 사용자 데이터 접근 불가
- 기존 프로젝트 ID가 어디에도 없음

### 단계 9. Day 2~14 콘텐츠 적용

작업:

- Day 1 엔진에 실제 Day 2~14 콘텐츠 입력
- 영어 공통팩
- 일본 국가팩
- 모든 문장에 phrase ID, 번역, 설명, 복습 설정
- 승인된 정적 음원 일괄 생성·연결
- Day 목록과 잠금·진행 상태
- Day 14 최종 역할극

완료 기준:

- Day 1~14 모두 같은 엔진으로 실행
- 임시 문구와 연결되지 않은 버튼 없음
- 모든 고정 문장에 자연·느린 음원 또는 명확히 표시된 개발 fallback 존재
- 완료된 locale만 선택 가능
- Day 14 완료 후 14일 결과 요약 표시
- 한국어 전체를 유창하게 한다는 과장 표현 없음

### 단계 10. PWA·오프라인·성능 정리

작업:

- web app manifest
- 기본 설치 가능성
- 정적 자산 캐시 전략
- 현재·다음 음원만 선로딩
- 오류 경계와 재시도
- 이미지·음원 용량 정리
- 모바일 네트워크에서 초기 화면 점검

완료 기준:

- 음원 하나의 실패가 수업 전체를 멈추지 않음
- 캐시된 수업과 음원이 가능한 범위에서 다시 재생됨
- 초기 화면이 전체 음원을 내려받지 않음
- service worker가 개발과 업데이트를 방해하지 않음

### 단계 11. 전체 흐름 테스트와 UI 다듬기

필수 E2E 시나리오:

1. 신규 비회원 온보딩 → Day 1 시작
2. Day 1 중간 종료 → 재접속 → 같은 step 재개
3. 자연·느린 음성 재생
4. 녹음 → 원본 비교 → 재녹음
5. 마이크 거절 → 건너뛰기 → 수업 완료
6. Day 1 완료 → 복습 생성
7. 복습 세션 완료 → 다음 복습일 저장
8. 이메일 로그인 → 비회원 진도 병합
9. 로그아웃 → 재로그인 → 복구
10. 다른 세션 → 클라우드 복구
11. 일본어·영어 전환 → 한국어 진도 유지
12. 네트워크 단절 → 로컬 저장 → 재연결 동기화
13. Day 1~14 모든 route와 음원 참조 검사

자동 검증:

- lint
- typecheck
- unit tests
- content validation
- audio validation
- production build
- Playwright E2E

완료 기준:

- 테스트 결과를 `docs/QA_REPORT.md`에 기록
- 실패한 테스트를 숨기지 않음
- 모바일 주요 화면을 직접 확인
- 로딩·빈 상태·오류·권한 거절 상태 확인
- 불필요한 임시 코드와 console error 제거

### 단계 12. 문서화와 인계

필수 문서:

```text
README.md
docs/REPO_AUDIT.md
docs/PRODUCT_SPEC.md
docs/ARCHITECTURE.md
docs/CONTENT_GUIDE.md
docs/AUDIO_PIPELINE.md
docs/AUDIO_LICENSES.md
docs/SUPABASE_SETUP.md
docs/COUNTRY_PACK_GUIDE.md
docs/QA_REPORT.md
docs/NEXT_ROADMAP.md
```

README에는 최소 다음을 포함한다.

- 설치
- 로컬 실행
- 테스트
- build
- 환경 변수
- Supabase 연결
- 음원 생성
- Day 추가
- locale·country pack 추가
- 현재 제한 사항

완료 후 기능 브랜치에 커밋하고, 권한이 있으면 push와 Draft PR을 만든다. 상용 배포와 main 병합은 하지 않는다.

---

## 15. 이번 범위에서 만들지 않을 것

다음은 현재 핵심 흐름이 완성되기 전에는 구현하지 않는다.

- 유료 결제
- 광고 시스템
- 실시간 서버 TTS
- 자유형 AI 채팅
- 신뢰 근거가 없는 발음 점수
- 사용자 녹음의 서버 저장
- 소셜 피드와 공개 랭킹
- 복잡한 코인·상점·아이템
- 실제 푸시 알림 발송
- 관리자용 대형 CMS
- Day 15~90의 실제 콘텐츠 전체
- 중국 본토 전용 배포
- 기존 Sites 주소 재사용
- 기존 `K_study` 연결

인터페이스만 있고 작동하지 않는 기능도 만들지 않는다.

---

## 16. 최종 완료 기준

다음 조건을 모두 만족해야 이번 목표를 완료했다고 보고할 수 있다.

### 제품 흐름

- 비회원이 60초 안에 온보딩을 완료한다.
- 앱 시작 후 3분 안에 첫 한국어 음성을 듣거나 녹음한다.
- Day 1을 처음부터 끝까지 완료할 수 있다.
- Day 1~14가 같은 공통 엔진으로 작동한다.
- 홈, 학습, 복습, 설정 메뉴가 실제 기능과 연결된다.
- 선택한 국가팩과 캐릭터가 실제 화면·설명·음성에 반영된다.

### 음성

- 고정 수업은 정적 AI 음원을 우선 재생한다.
- 자연 속도와 느린 속도가 구분된다.
- 원본과 내 녹음을 번갈아 들을 수 있다.
- 마이크를 거절해도 수업을 완료할 수 있다.
- 유료 TTS API 호출이 없다.
- 음원 metadata와 라이선스 상태가 기록된다.
- 상업적 사용 근거가 없는 음원은 승인 상태로 표시되지 않는다.

### 저장과 복구

- 비회원 진도가 브라우저 재시작 후 복구된다.
- 정확한 step에서 이어진다.
- Day 완료 후 복습 항목이 생성된다.
- 로그인 시 로컬 진도를 잃지 않고 병합한다.
- 새 Supabase가 연결된 경우 다른 세션에서 복구된다.
- 네트워크 실패가 진도 손실로 이어지지 않는다.

### 품질

- 모바일 주요 화면에서 겹침과 잘림이 없다.
- 의미 없는 버튼과 빈 페이지가 없다.
- TypeScript, lint, unit test, content validation, build가 통과한다.
- 핵심 E2E 시나리오가 통과한다.
- README와 설계·음원·Supabase·QA 문서가 있다.
- 기존 Supabase와 기존 Sites 주소가 사용되지 않는다.

새 Supabase 정보나 음원 라이선스 승인이 없어 일부 외부 검증을 완료할 수 없다면, 전체가 완료됐다고 표현하지 않는다. 구현 완료 부분과 외부 정보가 필요한 부분을 정확히 나누어 보고한다.

---

## 17. 최종 보고 형식

작업이 끝나면 다음 순서로 간결하지만 구체적으로 보고한다.

1. 구현한 사용자 흐름
2. 주요 기술 결정
3. 추가·변경한 핵심 파일
4. Day 1~14 콘텐츠와 국가팩 상태
5. 정적 TTS 모델·음원·라이선스 상태
6. 로컬 저장과 Supabase 연결 상태
7. 실행한 테스트와 결과
8. 직접 확인할 실행 방법
9. 남아 있는 제한 사항
10. Git 브랜치, 커밋, Draft PR 주소
11. 다음 권장 작업 3개

스크린샷 또는 짧은 화면 기록을 제공할 수 있으면 다음 장면을 포함한다.

- 온보딩 완료
- 홈
- 자연·느린 음성
- 녹음 비교
- 역할극
- 복습
- 로그인·동기화 상태

---

## 18. 실행 판단 요약

이 프로젝트의 개발 우선순위는 다음 한 줄로 정리된다.

> `Day 1 완전 동작 → 무료 정적 AI 음원과 녹음 비교 → 로컬 진도·복습 → 새 Supabase 동기화 → 같은 엔진으로 Day 2~14 → 일본 국가팩 → 전체 QA와 문서화`

실시간 TTS, 자유 대화, 결제, Day 15 이후 확장은 이 흐름이 검증된 다음에 진행한다.

---

## 19. 검토 참고 자료

다음 링크는 제품 방향과 TTS 후보를 검토할 때 참고한 자료다. 링크에 표시된 코드 또는 모델 라이선스만으로 학습 데이터의 상업적 사용 조건까지 확인됐다고 판단하지 않는다.

- [TEUIDA 미국 앱스토어: 3분 말하기와 1인칭 상황 연습](https://apps.apple.com/us/app/teuida-learn-languages/id1457532562)
- [LingoDeer 대만 앱스토어: 체계적 과정과 사용자 리뷰](https://apps.apple.com/tw/app/lingodeer-%E5%AD%B8%E7%BF%92%E6%97%A5%E6%96%87-%E9%9F%93%E6%96%87-%E8%8B%B1%E6%96%87%E7%AD%8913%E7%A8%AE%E5%A4%96%E5%9C%8B%E8%AA%9E%E8%A8%80/id1261193709)
- [Korean by Nemo 인도네시아 앱스토어: 정적 음원과 원본·녹음 비교](https://apps.apple.com/id/app/korean-by-nemo/id487077174)
- [Seyo 미국 앱스토어: 음성 인식과 AI 음성 관련 사용자 리뷰](https://apps.apple.com/us/app/seyo-learn-speak-korean/id6657977758)
- [Qwen3-TTS 공식 저장소](https://github.com/QwenLM/Qwen3-TTS)
- [MeloTTS 공식 저장소](https://github.com/myshell-ai/MeloTTS)
- [MeloTTS Korean 모델 카드](https://huggingface.co/myshell-ai/MeloTTS-Korean)
