# USER_FLOW

## 신규 사용자 흐름

1. 앱 첫 실행
2. 국가팩과 모국어 선택
3. 한국어 수준 선택
4. 학습 목적 선택
5. 하루 목표 선택
6. 튜터와 음성 선택
7. 알림 선호 시간 확인
8. Day 1 바로 시작

## 재방문 사용자 흐름

앱은 `localStorage`의 `korean-first-talk:user-state:v1`에서 온보딩, 수업 단계, 복습 항목을 불러온다. 온보딩이 완료된 사용자는 홈으로 이동하고, 진행 중인 Day 1은 마지막 `currentStepId`에서 이어진다.

## 비회원에서 회원 전환

내 정보 화면에서 이메일을 입력하면 비회원 진도가 로컬 계정 저장소 `korean-first-talk:cloud-profile:{email}`과 병합된다. 완료 수업, 더 많이 진행한 단계, 복습 항목이 보존된다.

## 수업 흐름

상황 제시 → 튜터 인사 → 핵심 문장 → 뜻 확인 → 자연 속도 듣기 → 느린 속도 듣기 → 나눠 듣기 → 녹음 → 비교 → 이해 문제 → 역할극 → 정리 → 복습 예약.

## 복습 흐름

Day 1 완료 후 오답, 힌트, 재생 횟수, 녹음 재시도 같은 지표로 복습 우선순위를 계산한다. 복습 완료 시 `success` 또는 `hard` 결과에 따라 다음 due 시간이 갱신된다.

## 오류·권한 거절 흐름

마이크 권한이 거절되면 해결 안내를 보여 주고 수업은 계속 진행한다. 녹음 API가 없는 환경에서는 녹음 단계를 건너뛸 수 있다. Supabase 정보가 없으면 로컬 저장 상태를 명확히 표시한다.
