console.log("Browser TTS voices are only available inside a browser runtime.");
console.log("Open the app, then run this in DevTools:");
console.log("speechSynthesis.getVoices().filter((voice) => voice.lang.toLowerCase().startsWith('ko'))");
